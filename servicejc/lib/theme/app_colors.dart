// lib/theme/app_colors.dart
import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF1A1A1A);
  static const Color secondary = Color(0xFF2C2C2C);
  static const Color accent = Color(0xFFFFD700);
  static const Color softWhite = Colors.white70;

  // Colores específicos
  static const Color error = Colors.red;
  static const Color iconButton = Colors.blue;
  static const Color floatingButton = Colors.blueAccent;

  // ServiciosScreen
  static const Color cardBackground = Colors.white;
  static const Color cardTitle = Color.fromRGBO(52, 73, 94, 1); // título de AppBar y cards
  static const Color cardSubtitle = Colors.green;
  static const Color elevatedButton = Color.fromRGBO(52, 152, 219, 1);
  static const Color elevatedButtonForeground = Colors.white;
  static const Color activeCheckbox = Color.fromRGBO(39, 174, 96, 1);

  static const Color white = Colors.white;
  static const Color white70 = Colors.white70;
  static const Color white54 = Colors.white54;
  static const Color success = Color(0xFF27AE60);
  static const Color danger = Color(0xFFE74C3C);
}
